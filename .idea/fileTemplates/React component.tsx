import {JSX} from 'react';

type ${NAME}Props = {
    
};

export function ${NAME}(props: Readonly<${NAME}Props>): JSX.Element {
    const {} = props;

    return (
        <>
            $0
        </>
    );
}
